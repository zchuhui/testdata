#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.0.54.5@stylus/bin/stylus" "$@"
  ret=$?
else 
  node  "$basedir/../.0.54.5@stylus/bin/stylus" "$@"
  ret=$?
fi
exit $ret
